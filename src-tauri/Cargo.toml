[package]
name = "codera"
version = "0.2.6"
description = "Codera for the desktop"
authors = ["Codera"]
license = ""
repository = ""
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "codera_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
tauri = { version = "2.11.3", features = ["tray-icon"] }
tauri-plugin-log = "2"
tauri-plugin-notification = "2"
tauri-plugin-opener = "2"

# Fullscreen on macOS works with AppKit's title strip directly.
[target.'cfg(target_os = "macos")'.dependencies]
objc2 = "0.6"
objc2-app-kit = { version = "0.3", default-features = false, features = ["std", "objc2-core-foundation", "NSButton", "NSControl", "NSResponder", "NSScreen", "NSView", "NSWindow"] }
